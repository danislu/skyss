import { expect } from 'chai';

describe('fixture 1', () => {
    it('test 1', () => {
        expect(true).to.equal(true);
    });

    it('test 2', () => {
        expect(false).to.equal(false);
    });
});
